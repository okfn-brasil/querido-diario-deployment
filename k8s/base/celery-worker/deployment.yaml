---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker
  namespace: querido-diario
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-worker
  template:
    metadata:
      labels:
        app: celery-worker
    spec:
      containers:
        - name: celery-worker
          image: ghcr.io/okfn-brasil/querido-diario-backend:latest
          command:
            - celery
            - -A
            - config
            - worker
            - -l
            - INFO
            # Sem --concurrency, o celery usa multiprocessing.cpu_count()
            # (o node inteiro, ex: 96) como número de processos prefork —
            # explodia o limite de memória em segundos (OOMKilled, exit 137,
            # ~2s após o start). Tarefas leves (alerts.tasks.*), não precisa
            # de mais que isso.
            - --concurrency=2
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: QD_BACKEND_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: QD_BACKEND_SECRET_KEY
            - name: QD_BACKEND_DB_URL
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: QD_BACKEND_DB_URL
            - name: MAILJET_API_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: MAILJET_API_KEY
            - name: MAILJET_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: MAILJET_SECRET_KEY
          resources:
            limits:
              memory: 768Mi
            requests:
              memory: 384Mi

# Arquivos de ambiente com credenciais - NAO VERSIONAR
# =====================================================
# O arquivo .env no repositório é um template (sem valores sensíveis).
# Crie uma cópia com os valores reais e nunca a versione.

.env.production
.env.local
.env.staging
.env.backup*
.env.*.backup

# Arquivos temporários de instrução (usados apenas durante setup inicial)
instructions-01.md
instructions-02.md

# Arquivos de prod capturados durante auditoria (não versionados)
prod-compose-dbs.yml
prod-compose-services.yml
prod-compose-services-env-vars
prod-traefik-services.yml

# Secrets K8s com valores reais (nunca versionar)
k8s/overlays/*/secret-app.env
k8s/overlays/*/*.secret.yaml
k8s/base/secret-app-values.yaml
